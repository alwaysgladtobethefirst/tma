---
"@skrynnyk/tma": minor
---

add the buttons telegram draws (`<MainButton>`, `<SecondaryButton>`, `<BackButton>`, `<SettingsButton>` and their hooks), a `<Tappable>` primitive that makes any child feel pressable, and haptics in the core
